# AI Usage Log — 2026-08-18/19 — EB-NeRD Contrastive-Vector vs. MiniLM (ADR-008 Addendum)

## Prompts (verbatim, in order)

### Prompt 1

> Current Objective: Test whether EB-NeRD's provided contrastive_vector artifact outperforms our from-scratch MiniLM embedding on local validation — ADR-008 rejected using EB-NeRD's provided artifacts for cross-dataset comparability, but that tradeoff's actual cost was never measured, only assumed.
>
> Download Ekstra_Bladet_contrastive_vector.zip (341MB, URL: https://ebnerd-dataset.s3.eu-west-1.amazonaws.com/artifacts/Ekstra_Bladet_contrastive_vector.zip) and inspect its format — confirm it's per-article vectors keyed by article ID, and check what it was actually trained on (read any accompanying documentation/README in the archive).
> Build a second semantic retrieval variant that substitutes this artifact for our MiniLM embeddings, reusing the exact same user-representation and evaluation code (mean-pooled history, same eval harness) — this isolates the embedding source as the only variable.
> Run it through the same local validation harness (AUC/MRR/nDCG, warm/cold slicing, bootstrap CIs) on ebnerd_small validation, and compare directly against the existing MiniLM numbers (AUC 0.5430).
> Report the result plainly — if the contrastive vector wins by a CI-clear margin, tell me and we'll decide about a second leaderboard submission. If it doesn't, that's also worth knowing and documenting.
> Don't touch the existing MIND/EB-NeRD shared-pipeline code or ADR-008's decision — this is an isolated additional experiment, write it up as a new ADR addendum either way.

### Prompt 2 (answer to a clarifying question about the stalled download)

> prepare this experiment to be done on kaggle

(Given in response to being asked how to proceed after the direct S3 download of `Ekstra_Bladet_contrastive_vector.zip` proved unreliable in this sandbox — see below.)

### Prompt 3 (2026-08-19, after the engineer ran the Kaggle notebook)

> Current Objective: Real contrastive-vector-vs-MiniLM results now exist at experiments/contrastive_vector_ebnerd_small_2026-08-18/{config,results}.json —do check that yourself - write the ADR-008 addendum and update PROJECT_STATE.md and the design note with the actual numbers, not placeholders.
>
> Verify the two JSON files parse and match this summary exactly: recall@50/100/200 = 0.58%/1.20%/2.47% (contrastive) vs 0.14%/0.43%/1.21% (MiniLM), all CI-clear wins. AUC 0.5453 (CI 0.5435–0.5471) vs 0.543 — CI-clear but narrow. MRR/nDCG@5/nDCG@10 — clear wins. Diversity@10: MiniLM wins (0.789 vs 0.780). Novelty@10: no clear winner (ci_clear_win: null) — don't report this as a win.
> Write the ADR-008 addendum honestly — contrastive vector wins on ranking accuracy, loses on diversity, ties on novelty. Not a blanket "the provided artifact is better."
> Update the design note's relevant sections (§2 choices, §6 limitations) to reflect this real measured tradeoff instead of the earlier unverified assumption.

## What happened this session

- Read `decisions/ADR-008-semantic-retrieval-design.md`, `src/retrieval/embed.py`, `src/retrieval/score.py`, `src/retrieval/retrieve.py`, `scripts/run_embed_experiment.py`, `scripts/run_ranking_eval.py`, and `src/evaluation/ranking_metrics.py`/`bootstrap.py` to understand the exact existing user-representation/scoring/harness code this experiment needed to reuse unchanged.
- Attempted to download `Ekstra_Bladet_contrastive_vector.zip` (341MB, real `Content-Length` confirmed via `curl -I`: 357,581,080 bytes) directly in this sandbox. Real, measured outcome: throughput swung between ~4KB/s and ~170KB/s across three attempts, one of which stalled and then hit `curl: (56) Recv failure: Connection reset by peer` after several hours; only ~214MB (63%) landed reliably, and the resulting file failed `unzip -l` (`End-of-central-directory signature not found` — confirmed truncated, not just slow). Per CLAUDE.md's Resource Availability clause, this was surfaced explicitly via `AskUserQuestion` rather than silently kept retrying or silently substituted with a lower-fidelity alternative — engineer chose "prepare this experiment to be done on Kaggle" (Prompt 2 above).
- Before writing any loading code against assumed columns, checked official documentation (WebSearch + WebFetch against `ebanalyse/ebnerd-benchmark`'s real GitHub source, specifically `examples/reproducibility_scripts/ebnerd_nrms_docvec.py` and `args_nrms_docvec.py`) rather than guessing the artifact's schema. Confirmed: the archive contains a single `Ekstra_Bladet_contrastive_vector/contrastive_vector.parquet`; the project's own official loading convention (`create_article_id_to_value_mapping(df=df_articles, value_col=df_articles.columns[-1])`) uses the **last column** as the embedding vector, with an article-id column separate from it. No public documentation of the *training methodology* (base model, contrastive objective, which text fields) was found anywhere outside the archive itself — flagged as a real documentation gap, not filled in with inference; the notebook's own `inspect_artifact` cell will print whatever accompanying docs the real archive actually ships, and that's the authoritative source once it's run.
- Wrote `scripts/run_contrastive_vector_experiment.py` — the one new function is `load_contrastive_index` (builds an `EmbeddingIndex`, the *same* dataclass `embed.py` defines, from the provided artifact instead of encoding text). Everything downstream (`build_user_embedding_query`, `EmbeddingScorer`, `embed_retrieve_top_k`, `recall_at_k`, every Q4 ranking metric + bootstrap CI) is imported and reused with zero modification, per the brief's explicit "isolate the embedding source as the only variable" requirement. `src/retrieval/embed.py`/`score.py`/`retrieve.py`, `scripts/run_embed_experiment.py`, `scripts/run_ranking_eval.py`, and ADR-008 itself were not touched.
- **Smoke-tested locally before shipping to Kaggle**, using a synthetic fake `contrastive_vector.parquet` (random 768-dim vectors, 90% coverage of the real local `ebnerd_small` corpus by construction, to stress-test the incomplete-coverage path) run through the *actual* `run()` end-to-end against the real local `ebnerd_small` validation data. This caught a real bug before it could cost a slow Kaggle round-trip: `score.py::_lookup_scores` (shared, unmodified) scores an uncovered candidate as `-inf` by design, which is fine for `rank_candidates`/`mrr`/`ndcg_at_k` but crashes `sklearn.roc_auc_score` inside `safe_auc` (rejects non-finite input) — a path MiniLM's 100%-by-construction coverage never exercised and no existing test hit at more than one stray id. Fixed with a new, local-only `_finite_scores_for_auc` helper in the new script (substitutes each impression's own minimum finite score minus 1 for `-inf`, preserving the identical rank order without introducing infinity) — not a change to `score.py`'s contract. Re-ran the same synthetic smoke test after the fix: completed end-to-end, random vectors correctly produced AUC ≈ 0.50 (i.e., no signal) exactly as expected for a random-vector sanity check, confirming the harness plumbing itself is correct.
- Since the download can't complete in this sandbox, prepared the Kaggle path instead (Kaggle's network doesn't have this problem, and the project already has a precedent for this handoff — Part 0/Part 2 of the EB-NeRD Codabench work): `notebooks/ebnerd_contrastive_vector_src_bundle.zip` (the exact `src/` subtree this experiment needs — `datasets/{ebnerd,mind}.py`, `pipeline/*`, `utils/*`, `retrieval/*`, `evaluation/*` — plus `scripts/run_bm25_experiment.py` and the new `scripts/run_contrastive_vector_experiment.py`, ~42KB) and `notebooks/ebnerd_contrastive_vector_kaggle_run.py` (paste-into-cells orchestration: download the two real S3 zips on Kaggle's fast network, rebuild `ebnerd_small` via the project's own unmodified `build_ebnerd_bundle` — guaranteeing byte-identical schema to what produced the MiniLM baseline — inspect the real artifact, run the full comparison, print a summary to relay back, package just the two small result JSONs for download).
- **2026-08-19: the engineer ran the Kaggle notebook and relayed real results** at `experiments/contrastive_vector_ebnerd_small_2026-08-18/{config,results}.json`. Verified both files parsed and checked every number in the engineer's summary (Prompt 3) against the actual JSON content digit-for-digit before writing anything downstream, per this project's own "verify before handing back" discipline — all matched exactly (recall@50/100/200, AUC + its CI, MRR/nDCG@5/nDCG@10, Diversity@10's loss, Novelty@10's null/tie). Real coverage came back 100% (20,738/20,738 local articles; artifact's full catalog 125,541 articles, 768-dim vectors) — the earlier `-inf`/AUC fix never actually fired on this run, but would have crashed it without the fix.
- Wrote the actual ADR-008 Addendum (`decisions/ADR-008-semantic-retrieval-design.md`) with the honest mixed result: CI-clear win on recall@K and every Q4 accuracy metric, CI-clear *loss* on Diversity@10, tie on Novelty@10 — explicitly not framed as a blanket "the provided artifact is better," and explicitly not reversing ADR-008's own decision (the artifact only covers EB-NeRD).
- Updated `docs/design_note.md` §2 (Choices) and §6 (Limitations) with the real numbers, then mirrored both edits into `docs/design_note.tex` and recompiled. **The addition initially pushed the PDF to 5 real pages** (`pypdf` caught this; a stale `mdls`/Spotlight cache reading of "4 pages" on the freshly-rewritten file was misleading and not trusted) — trimmed both additions, then found the trimming alone wasn't enough and reduced the margin (1in → 0.85in) to recover a real page's worth of room, which surfaced three overfull-hbox LaTeX warnings (one pre-existing, one in each of the two new blocks) that hadn't been visible at the wider margin; fixed all three with `sloppypar` rather than shipping a PDF with text bleeding into the margin. Final state re-verified directly (not assumed): `pypdf` reports 4 pages, `mdls` now agrees, zero compiler warnings.
- Updated `PROJECT_STATE.md`: Component Status (Semantic Retrieval row), Recent Decisions (new ADR-008 addendum-2 row), a full new Session Notes entry, a new Next Actions item flagging the second-leaderboard-submission decision as open, and the top summary block — done additively, without touching the pre-existing uncommitted Aug 14-18 content already in the file, and explicitly flagging (not silently fixing) that the Aug 14 entry's staleness relative to Aug 16/18 work was out of this session's scope.
- Extended this log (rather than replacing it) with this final prompt and outcome.

## What was AI-generated vs. human-written/edited

- AI-generated, 2026-08-18: `scripts/run_contrastive_vector_experiment.py`, `notebooks/ebnerd_contrastive_vector_src_bundle.zip`, `notebooks/ebnerd_contrastive_vector_kaggle_run.py`. All local smoke-testing (synthetic-artifact construction, the two `run()` invocations, the bug diagnosis and fix) was AI-executed.
- AI-generated, 2026-08-19: the ADR-008 Addendum text, the `design_note.md`/`.tex` edits and PDF recompilation/page-count fixes, the `PROJECT_STATE.md` edits, this log file (all sessions).
- Human/engineer: the three prompts above; the decision to redirect to Kaggle; running `notebooks/ebnerd_contrastive_vector_kaggle_run.py` on Kaggle and relaying `experiments/contrastive_vector_ebnerd_small_2026-08-18/{config,results}.json` back — outside this sandbox's reach by design, same as the project's prior Kaggle handoffs; the engineer's own summary of the results (independently verified against the raw JSON, not taken on trust) is quoted in Prompt 3 above.
- Nothing in `src/retrieval/embed.py`, `score.py`, `retrieve.py`, `scripts/run_embed_experiment.py`, `scripts/run_ranking_eval.py`, or ADR-008's pre-existing (non-Addendum) content was modified at any point.
