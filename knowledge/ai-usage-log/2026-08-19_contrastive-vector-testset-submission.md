# AI Usage Log — 2026-08-19 — EB-NeRD Contrastive-Vector Testset Submission (In Progress)

## Prompts (verbatim, in order)

### Prompt 1

> Current Objective: Generate a real EB-NeRD leaderboard submission using the contrastive vector method (just verified to beat MiniLM on local validation) over the actual blind test set, and package it for Codabench.
>
> Find whatever script/pipeline originally generated prediction.txt for the MiniLM submission (ID 888045, score 0.5404) against ebnerd_testset — that's the known-working reference for format, since it already passed Codabench validation once.
> Build a Kaggle-ready version of that same pipeline with one substitution: swap MiniLM's embedding index for load_contrastive_index (already built, verified, and working — same function used in the validation run) over the same Ekstra_Bladet_contrastive_vector.zip artifact.
> Same constraints as before: ebnerd_testset is too large for local, needs Kaggle's network/compute. Package it the same way as the earlier Kaggle notebook — cells in order, a bundled src/ subtree, clear instructions for what to run and what to bring back.
> Output should be prediction.txt, zipped, in the exact format Codabench's evaluate.py expects (verify against the copy I uploaded earlier) — confirm the format matches before calling it done, not after a failed upload.
> Tell me plainly when it's ready to run on Kaggle, same handoff as last time — I'll run it and bring back whatever it produces.

## What happened this session

- Found the known-working reference: `notebooks/ebnerd_part2_kaggle_test_run.py` — the exact notebook that produced `submissions/ebnerd_testset_embed/prediction.zip` (already uploaded, leaderboard score 0.5404, submission ID 888045, per `PROJECT_STATE.md`'s prior session notes). Read it in full.
- **Verified the real, already-uploaded reference file directly** rather than trusting the notebook's own comments about the format: unzipped `submissions/ebnerd_testset_embed/prediction.zip`, confirmed a single root-level `predictions.txt`, 13,536,710 lines, each `{impression_id} [rank_1,...,rank_N]` (1-indexed permutation, JSON array, no spaces after commas). Also read `evaluation/official/evaluate.py`'s `parse_line`/`scoring` functions directly to confirm this is exactly the shape the grader parses, and that lines must appear in the same order as the truth file (matched by position, not looked up by id) — confirming why the shared `iter_raw_impressions*` functions' "never re-sort" design is load-bearing, not incidental.
- Read `src/submission/ebnerd_format.py` in full (`iter_raw_impressions_from`, `ranks_for_impression`, `sample_raw_impressions`) — confirmed this project's own official-format converter, unchanged and reused, not reimplemented.
- Built `notebooks/ebnerd_contrastive_vector_testset_kaggle_run.py` — cell-for-cell mirror of the reference notebook's structure (discovery, memory check, the benchmark-then-decide gate, checkpointed full run, line-count/format validation, packaging), with exactly one substitution: Cell 4 calls `load_contrastive_index` (imported unchanged from `scripts/run_contrastive_vector_experiment.py` — the identical function already verified in the local `ebnerd_small` validation comparison) instead of `build_embedding_index`. Everything else — `EmbeddingScorer`, `build_user_embedding_query`, the checkpoint/resume logic, `src/submission/ebnerd_format.py`'s functions — is imported and reused unmodified. Flagged two real, honest differences from the reference run rather than silently carrying its numbers forward: (1) no GPU is needed this time (no encode step — the vectors are provided, not computed), so the notebook recommends leaving the accelerator off to save Kaggle GPU-hour quota; (2) 768-dim vectors cost roughly 2x a 384-dim dot product, so Cell 6's real benchmark — not the reference run's ~12.80h — is what the engineer should read before setting `RUN_FULL_JOB`.
- Built `notebooks/ebnerd_contrastive_vector_testset_src_bundle.zip` — a leaner bundle than the local-validation one (adds `src/submission/ebnerd_format.py`; drops `src/pipeline/*` and `src/datasets/mind.py`, neither of which this call graph touches, verified by tracing every import chain rather than copying the whole `src/` tree out of caution). Import-verified locally (`python -m py_compile` + a live import of every module the bundle needs, including `load_contrastive_index`/`inspect_artifact` from the bundled experiment script) before treating it as ready.
- **Full end-to-end dry run before calling this done** (per the prompt's own explicit "confirm the format matches before calling it done, not after a failed upload"): ran the actual `load_contrastive_index` → `build_user_embedding_query` → `EmbeddingScorer` → `iter_raw_impressions_from`/`ranks_for_impression` → file-write chain — the same real code Cells 4/5/7 use — against the real local `ebnerd_small.zip`'s `validation` split (244,647 real impressions, `has_labels=False` to exercise the actual blind-test code path) with a synthetic full-coverage contrastive artifact. Result: 0 malformed lines across all 244,647, and the produced line shape (`'96791 [2,1,4,3,5]'`) matches the real uploaded reference file's shape (`'6451339 [3,7,8,1,9,6,5,4,2]'`) exactly, side by side.
- Fixed one rough edge found while writing Cell 5: an initial draft looked up `build_user_embedding_query` via `load_contrastive_index.__globals__[...]` instead of a direct import — corrected to a plain `from src.retrieval.embed import build_user_embedding_query` before shipping.

## What was AI-generated vs. human-written/edited

- AI-generated, this session: `notebooks/ebnerd_contrastive_vector_testset_kaggle_run.py`, `notebooks/ebnerd_contrastive_vector_testset_src_bundle.zip`, this log file. All verification (reading the real reference `prediction.zip` and `evaluate.py`, the import checks, the full end-to-end local dry run) was AI-executed.
- Human/engineer: the prompt above; running the Kaggle notebook itself and relaying `predictions_contrastive.txt`/`prediction_contrastive.zip` (or Cell 6's benchmark output, if `RUN_FULL_JOB` needs a decision first) back is still pending and will be the engineer's own action, outside this sandbox's reach by design, same as every prior Kaggle handoff in this project. Submitting to Codabench and screenshotting the result is also the engineer's own action on their own account.
- Nothing in `src/submission/ebnerd_format.py`, `src/retrieval/embed.py`/`score.py`/`retrieve.py`, `scripts/run_contrastive_vector_experiment.py`, or `notebooks/ebnerd_part2_kaggle_test_run.py` (the reference) was modified this session.
